public class MaxForkJoin {
    /**
     * Find the max of the elements of an array.
     *
     * @param arr array to sum
     * @return sum of the array's elements
     * @throws InterruptedException shouldn't happen
     */
    public static double max(int[] arr) throws InterruptedException {
        return 0;
    }
}
